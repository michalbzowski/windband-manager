spring:
  application:
    name: windband-manager

  datasource:
    url: jdbc:h2:mem:windband;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    open-in-view: false
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

  flyway:
    enabled: false

  thymeleaf:
    cache: false

  security:
    oauth2:
      resourceserver:
        jwt:
          # Use jwk-set-uri for testing to avoid issuer validation issues
          jwk-set-uri: http://localhost:8180/realms/windband/protocol/openid-connect/certs

server:
  port: 8080

app:
  jwt:
    secret: testSecretKeyForLocalDevelopmentThatIsLongEnough256Bits12345678901234567890
    expiration-ms: 86400000
  base-url: http://localhost:8080
  keycloak:
    url: http://localhost:8180
    realm: windband